% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{positive}
\alias{positive}
\alias{simplex}
\alias{corr_matrix}
\alias{constraints}
\title{Constraint Specification for Parameters}
\usage{
positive(name)

simplex(name, k)

corr_matrix(name, dim)

constraints(...)
}
\arguments{
\item{name}{Character string naming the parameter(s) to constrain}

\item{k}{For \code{simplex()}, the number of elements in the simplex}

\item{dim}{For \code{corr_matrix()}, the dimension of the correlation matrix}

\item{...}{For \code{constraints()}, constraint objects to combine}
}
\value{
An object of class \code{autodiffr_constraint} or \code{autodiffr_constraints}
}
\description{
These functions create constraint specifications that can be used in
\code{optim_mle()} to enforce parameter constraints via smooth reparameterizations.
}
\details{
Constraints are enforced via smooth reparameterizations:
\itemize{
\item \code{positive()}: Uses exponential transformation, so unconstrained
parameter \code{u} maps to constrained \code{theta = exp(u)}
\item \code{simplex()}: Uses softmax transformation, so unconstrained
parameters \code{u} map to constrained \code{theta = softmax(u)} (non-negative and sum to 1)
\item \code{corr_matrix()}: Not yet implemented (will use Cholesky decomposition)
}

When constraints are applied, the log-likelihood is adjusted by the log-Jacobian
of the transformation to maintain correct inference.
}
\examples{
\dontrun{
# Positive constraint for variance parameter
constr <- positive("sigma")

# Simplex constraint for mixture weights
constr <- simplex("weights", k = 3)

# Combine constraints
constr_list <- constraints(
  positive("sigma"),
  simplex("weights", k = 3)
)
}
}
