% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov_info.R
\name{vcov_info}
\alias{vcov_info}
\alias{vcov_info.autodiffr_fit}
\title{Extract Variance-Covariance Matrix with Diagnostics}
\usage{
vcov_info(object, ...)

\method{vcov_info}{autodiffr_fit}(object, type = c("observed", "expected", "sandwich"), ...)
}
\arguments{
\item{object}{An object of class \code{autodiffr_fit}}

\item{...}{Additional arguments (currently ignored)}

\item{type}{Character string specifying the type of variance-covariance matrix.
Options: \code{"observed"} (default), \code{"expected"}, or \code{"sandwich"}.}
}
\value{
An object of class \code{autodiffr_vcov_info} containing:
\item{vcov}{Variance-covariance matrix}
\item{info}{Information matrix (negative Hessian)}
\item{eigenvalues}{Eigenvalues of the information matrix}
\item{cond_number}{Condition number of the information matrix}
\item{type}{Type of variance-covariance matrix requested}
\item{method}{Estimation method ("mle" or "mest")}
\item{nobs}{Number of observations (if available)}
}
\description{
Computes and returns the variance-covariance matrix along with diagnostic
information such as eigenvalues, condition number, and information matrix.
}
\examples{
\dontrun{
library(torch)
set.seed(123)
data <- rnorm(100, mean = 5, sd = 2)

loglik_r <- function(theta, data) {
  mu <- theta["mu"]
  sigma <- theta["sigma"]
  sum(dnorm(data, mean = mu, sd = sigma, log = TRUE))
}

start <- c(mu = 0, sigma = 1)
fit <- optim_mle(loglik_r, start, data)

vcov_info(fit)
vcov_info(fit, type = "observed")
}
}
