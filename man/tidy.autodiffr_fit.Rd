% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom-methods.R
\name{tidy.autodiffr_fit}
\alias{tidy.autodiffr_fit}
\title{Tidy method for autodiffr_fit objects}
\usage{
tidy.autodiffr_fit(x, ...)
}
\arguments{
\item{x}{An object of class \code{autodiffr_fit}}

\item{...}{Additional arguments (currently ignored)}
}
\value{
A tibble with columns:
\item{term}{Parameter name}
\item{estimate}{Parameter estimate}
\item{std.error}{Standard error}
\item{statistic}{Z-statistic (estimate / std.error)}
\item{p.value}{Two-sided p-value}
}
\description{
Returns a tibble with parameter estimates, standard errors, test statistics,
and p-values. This method works with the \code{broom} package for tidy model output.
}
\details{
This function requires the \code{broom} and \code{tibble} packages. The \code{broom} package
is in Suggests, so it may not be installed. This method will work even if
\code{broom} is not attached, as it uses S3 method dispatch.
}
\examples{
\dontrun{
library(broom)
library(torch)

# Fit a model
fit <- optim_mle(loglik, start, data)

# Get tidy output
tidy(fit)
}
}
